// ─── Config template — commit this, not config.js ─────────────────────────────
// 1. Copy this file: cp config.example.js config.js
// 2. Paste your OpenRouter key below
// 3. config.js is gitignored so your key stays private

const PRIVATE_CONFIG = {
  openRouterKey: 'your-openrouter-key-here',  // get one at openrouter.ai/keys
};
